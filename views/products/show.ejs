<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Sporting Goods Store</title>
</head>
<body>
    <h1>Sports R Us</h1>
    <a href="/product"><button>Back to Home Page</button></a>
    <main>
        <article class="show">
            <img src="<%= product.img %>"/>
            <h2><%= product.name %></h2>
            <h4>$<%= product.price %></h3>
            <p><%= product.description %></p>
            <h4>In Stock:<%= product.qty %></h4>
            <% if (product.qty > 0) { %>
                <a href="/product/purchase/<%= product._id %>"><button>Purchase</button></a>
            <% } else { %>
                <button class = "no-stock">Out of Stock</button>
            <% } %>
            <a href ="/product/<%= product._id %>/edit"><button>Edit Product</button></a>
            <form action="/product/<%= product._id %>?_method=DELETE" method="POST">
                <input type="submit" value="Delete Product" />
            </form>
        </article>
    </main>
</body>
</html>